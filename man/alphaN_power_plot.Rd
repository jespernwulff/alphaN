% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alphaN_power.R
\name{alphaN_power_plot}
\alias{alphaN_power_plot}
\title{Plot power across sample sizes at the calibrated alpha}
\usage{
alphaN_power_plot(
  d = c(0.1, 0.5),
  BF = 3,
  max = 10000,
  methods = c("JAB", "min", "robust", "balanced"),
  de = 0.5,
  ref = 0.05
)
}
\arguments{
\item{d}{Standardized effect sizes to draw, one panel per element, on
the scale of \code{\link[=alphaN_power]{alphaN_power()}}'s \code{d}. Defaults to c(0.1, 0.5).}

\item{BF}{Target Bayes factor for the calibration. Defaults to 3.}

\item{max}{The maximum number of sample size. Defaults to 10,000.}

\item{methods}{Character vector with the methods to draw, any subset of
c("JAB", "min", "robust", "balanced", "ES", "moment"). Defaults to the
four prior-fraction methods, matching the behavior of earlier package
versions.}

\item{de}{The prespecified (targeted) effect size in standardized units:
Cohen's d for \code{q = 1} and Cohen's f for joint tests (the scales coincide
at \code{q = 1}). Only used by methods "ES" and "moment". Defaults to 0.5, a
medium effect; use 0.2 for small and 0.8 for large effects (Cohen, 1988).}

\item{ref}{A fixed significance level drawn as a dashed reference curve,
or NULL to omit it. Defaults to 0.05.}
}
\value{
Prints a plot.
}
\description{
Draws, for each requested effect size, the power of the two-sided
single-coefficient test as a function of the sample size, where every
calibration method is evaluated at its own alpha level for the given
Bayes factor target. A fixed reference level (default 0.05) is drawn as
a dashed curve. This is the design-time companion of \code{\link[=alphaN_plot]{alphaN_plot()}}:
one figure shows what each calibration costs in power. Colors follow the
colorblind-safe Okabe-Ito palette; the effect-size and moment curves use
the same log-spaced spline interpolation as \code{\link[=alphaN_plot]{alphaN_plot()}}.
}
\examples{
# The power cost of evidence calibration for a small and a medium effect
alphaN_power_plot(d = c(0.1, 0.5), BF = 3,
                  methods = c("JAB", "balanced", "moment"), max = 2000)
}
\seealso{
\code{\link[=alphaN_power]{alphaN_power()}}, \code{\link[=alphaN_plot]{alphaN_plot()}}
}
